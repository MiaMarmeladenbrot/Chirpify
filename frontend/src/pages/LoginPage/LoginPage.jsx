// isEmailVerified === false => Navigate to Setting to verify Email
// isEmailVerified === true => Navigate to FeedPage

const LoginPage = () => {
  return <div>LoginPage</div>
}

export default LoginPage
